# Auto-generated by JOS charter sync. DO NOT EDIT.
# Generated at: 2026-05-17T19:37:17.106699+00:00
# Purpose: JOS cross-venture awareness index (per JOS-SPEC-018). Agents: read awareness-index.yaml to see what other ventures in the portfolio OFFER and NEED before starting parallel work that might duplicate an existing capability.
# Source of truth: jordan-operating-system repo.
# Edits to this file will be overwritten on the next sync.

# JOS Cross-Venture Awareness — Federation Configuration
# Per JOS-D0065 (Cross-Venture Awareness Lens) + JOS-SPEC-018
# (Venture Awareness Manifest). Federation owner: JOS.
#
# Architecture: per-venture files live at distribution/awareness/
# <venture>.yaml (authored by the venture maintainer). JOS aggregator
# (scripts/jos_tools/awareness_aggregator.py) reads them and produces
# awareness/awareness-index.yaml. That aggregated index is then
# projected to every consumer's .jos/awareness/ via the sync agent.
#
# Deliberately NOT called "fleet" — that term is reserved for the
# Foundry hardware fleet (Tailscale mesh of 6 machines) and the LLM
# roster fleet (model routing). Awareness is its own concept:
# portfolio-level offers + needs across all JOS-governed ventures.

awareness_id: jos-cross-venture-awareness
name: JOS Cross-Venture Awareness Index
description: >
  Authoritative aggregated catalog of what every JOS-governed venture
  OFFERS for consumption and what it NEEDS that another venture might
  provide. Agents landing in any venture can read the projected
  index to answer "what's in the portfolio, who offers what, who
  needs what" without round-trips to GitHub or asking Tim.

schema_version: "1.0"
schema_file: "_schema.yaml"
federation_owner: jos
owner: tim
default_steward: claude
review_cadence: quarterly

# Input source — per-venture files authored by venture maintainers.
input_glob: "../distribution/awareness/*.yaml"

# Aggregation output — produced by awareness_aggregator.py.
aggregated_output: "awareness-index.yaml"

# What gets projected to consumers (excludes the raw per-venture
# inputs in distribution/awareness/, includes only the aggregated
# view + this config + schema).
projection_includes:
  - _config.yaml
  - _schema.yaml
  - awareness-index.yaml
projection_excludes:
  - rendered/
