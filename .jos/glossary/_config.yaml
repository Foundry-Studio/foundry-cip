# Auto-generated by JOS charter sync. DO NOT EDIT.
# Generated at: 2026-05-17T19:37:17.102188+00:00
# Purpose: JOS federated glossary module (per JOS-SPEC-013). Agents: use the canonical prefix:term IDs declared here; consult _polysemy-ban-list.yaml before inventing cross-domain terms.
# Source of truth: jordan-operating-system repo.
# Edits to this file will be overwritten on the next sync.

# JOS Glossary — Federation Configuration
# Per JOS-D0056 (Federated Glossary Architecture) + JOS-SPEC-013
# (Glossary Federation File Format). Federation owner: JOS.
#
# Architecture: core + 10 domain modules with full-name prefixes
# (no abbreviations per JOS-S31 v1.1). FIBO-style across-module
# canonical-ID uniqueness enforced at build time.

glossary_id: jos-glossary
name: JOS Federated Governance Glossary
description: >
  Authoritative federated vocabulary for the Jordan Operating System.
  JOS owns the federation contract (prefix namespaces, schema, polysemy
  bans, CI rules). Domain content can be authored elsewhere (e.g., the
  World Standard library authors the `business:` namespace) as long as
  it declares JOS as federation parent. Agents consume via the build-
  generated flat index at glossary/rendered/glossary.index.json.

schema_version: "2.0"  # Major bump from 1.x monolithic to 2.x federated.
schema_file: "_schema.yaml"
polysemy_ban_list: "_polysemy-ban-list.yaml"
federation_owner: jos
owner: tim
default_steward: claude
review_cadence: quarterly

file_strategy: federated  # was: single (v1.x)

modules:
  - prefix: core
    name: Cross-Cutting Core Terms
    path: core.yaml
    polysemous_terms_banned: true
    content_owner: jos
    description: >
      Cross-cutting terms every domain depends on (e.g., owner, status,
      flexibility). The 5 polysemous terms in `_polysemy-ban-list.yaml`
      are forbidden here — they must be qualified per domain.

  - prefix: meta
    name: Meta-Governance
    path: domains/meta.yaml
    extends: core
    domain: meta
    content_owner: jos

  - prefix: engineering
    name: Engineering
    path: domains/engineering.yaml
    extends: core
    domain: engineering
    content_owner: jos

  - prefix: documentation
    name: Documentation
    path: domains/documentation.yaml
    extends: core
    domain: documentation
    content_owner: jos

  - prefix: data
    name: Data
    path: domains/data.yaml
    extends: core
    domain: data
    content_owner: jos

  - prefix: finance
    name: Finance
    path: domains/finance.yaml
    extends: core
    domain: finance
    content_owner: jos

  - prefix: strategy
    name: Strategy
    path: domains/strategy.yaml
    extends: core
    domain: strategy
    content_owner: jos

  - prefix: operations
    name: Operations
    path: domains/operations.yaml
    extends: core
    domain: operations
    content_owner: jos

  - prefix: people
    name: People
    path: domains/people.yaml
    extends: core
    domain: people
    content_owner: jos

  - prefix: legal
    name: Legal
    path: domains/legal.yaml
    extends: core
    domain: legal
    content_owner: jos

  - prefix: business
    name: Business Operations
    path: domains/business.yaml
    extends: core
    domain: business
    content_owner: library-atom
    federated_content_source:
      type: library_atom
      ref: libraries/standards/general-business-standard/glossary/glossary.yaml
      note: >
        World Standard library authors business-operations content under
        JOS federation. JOS owns prefix + schema + CI rules; WS owns
        term content. Per JOS-D0056.

# Legacy compatibility — migration window per JOS-SPEC-013 §10
legacy_compatibility:
  legacy_file: glossary.yaml
  migration_pending: true
  migration_scope: "JOSW2-670-phase-3-glossary-migration"
  coexistence_window_ends: 2026-08-16
  legacy_scheme_mapping:
    jos-governance: governance   # legacy scheme → new prefix
    engineering: engineering
    foundry-ventures: business

# Concept-scheme record (legacy field, retained for backward compat
# in v1.x consumers; v2.x consumers should use `modules:` above)
concept_schemes:
  - id: jos-governance
    name: JOS Governance Terms
    description: >
      Legacy v1.x scheme. Maps to `governance:` prefix in v2.x federation.
    term_prefix: "jos-"
    status: legacy
    superseded_by: governance
