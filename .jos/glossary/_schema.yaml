# Auto-generated by JOS charter sync. DO NOT EDIT.
# Generated at: 2026-05-17T19:37:17.102188+00:00
# Purpose: JOS federated glossary module (per JOS-SPEC-013). Agents: use the canonical prefix:term IDs declared here; consult _polysemy-ban-list.yaml before inventing cross-domain terms.
# Source of truth: jordan-operating-system repo.
# Edits to this file will be overwritten on the next sync.

# JOS Glossary — Term Schema v1.0
# Adapted from Foundry's general-business-standard glossary schema.
# SKOS-aligned field structure; simplified to JOS-specific needs.
#
# SKOS mapping:
#   canonical_name  → skos:prefLabel
#   short_definition→ skos:definition (summary)
#   definition      → skos:definition (full)
#   concept_scheme  → skos:inScheme
#   term_id         → skos:notation
#   broader_term    → skos:broader
#   narrower_terms  → skos:narrower
#   related_terms   → skos:related

schema_version: "1.0"

fields:

  # ── IDENTIFICATION (required) ──────────────────────────────────

  - name: term_id
    type: string
    required: true
    description: "Machine-readable unique identifier. Kebab-case, lowercase."
    validation: "^[a-z][a-z0-9-]*$"
    example: "jos-sop"

  - name: canonical_name
    type: string
    required: true
    description: "Preferred human-readable name. Title case. Maps to skos:prefLabel."
    example: "Standard Operating Procedure (SOP)"

  - name: short_definition
    type: string
    required: true
    description: "One-sentence tooltip-length summary. Max 200 characters."

  - name: definition
    type: string
    required: true
    description: "Full definition. 1-3 sentences. Maps to skos:definition."

  # ── CLASSIFICATION (required) ──────────────────────────────────

  - name: concept_scheme
    type: string
    required: true
    description: "Which concept scheme this term belongs to (from _config.yaml)."
    example: "jos-governance"

  - name: term_type
    type: string
    required: true
    enum: [structural_concept, governance_mechanic, lifecycle_state, relationship]
    description: "Faceted category (orthogonal to concept_scheme)."

  - name: axis
    type: string
    required: true
    enum: [thing, action, both]
    description: >
      Which holonic axis this term belongs to.
      thing = IS axis (noun, what exists).
      action = HAPPENS axis (verb, what occurs).
      both = lives on both axes independently.

  - name: status
    type: string
    required: true
    enum: [draft, approved, deprecated, retired]
    description: "Lifecycle state of this term's definition."

  # ── RELATIONSHIPS (optional) ───────────────────────────────────

  - name: broader_term
    type: string
    description: "term_id of a broader term. Maps to skos:broader."

  - name: narrower_terms
    type: array
    items: { type: string }
    description: "term_ids of narrower terms. Maps to skos:narrower."

  - name: related_terms
    type: array
    items: { type: string }
    description: "term_ids of non-hierarchical related terms. Maps to skos:related."

  - name: references
    type: array
    items: { type: string }
    description: "JOS object IDs that define or motivate this term (e.g., JOS-D0039)."

  # ── EXTENDED (optional) ────────────────────────────────────────

  - name: synonyms
    type: array
    items: { type: string }
    description: "Alternative labels. Maps to skos:altLabel."

  - name: scope_note
    type: string
    description: "Disambiguation: what this term does/does not cover."
