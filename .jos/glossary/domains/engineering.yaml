# Auto-generated by JOS charter sync. DO NOT EDIT.
# Generated at: 2026-07-12T21:03:32.610504+00:00
# Purpose: JOS federated glossary module (per JOS-SPEC-013). Agents: use the canonical prefix:term IDs declared here; consult _polysemy-ban-list.yaml before inventing cross-domain terms.
# Source of truth: jordan-operating-system repo.
# Edits to this file will be overwritten on the next sync.

# JOS Federated Glossary — Engineering Domain Module
# Per JOS-SPEC-013. Successor to legacy `concept_scheme: engineering`.

prefix: engineering
name: Engineering
extends: core
domain: engineering
description: >
  Engineering vocabulary — code, infrastructure, deployment, technical
  architecture. ~163 terms migrate here from legacy glossary.yaml
  during the JOSW2 670 Phase 3 sub-scope.
federation_owner: jos
content_owner: jos
schema_version: "2.0"
last_modified: 2026-07-12
last_reviewed: 2026-05-16
review_cadence: quarterly

terms:
  - term_id: domain
    canonical_id: engineering:domain
    canonical_name: "Engineering Bounded Context"
    pref_label: "domain (engineering)"
    short_definition: "A DDD-style bounded context within a system's architecture."
    definition: >
      The Domain-Driven Design sense — a bounded context with its
      own ubiquitous language, models, and integration boundary.
      Distinct from governance:domain (one of JOS's 10 governance
      domains) and business:domain (organizational area).
    see_also:
      - canonical_id: governance:domain
        relation: relatedMatch
      - canonical_id: business:domain
        relation: relatedMatch
    status: approved
    axis: IS
    term_type: structural_concept

  # FAS canon layer-rule vocabulary — locked with Tim 2026-07-11; promoted
  # to JOS governed vocabulary 2026-07-12 (JOS project cf848d96).
  # Source: FAS CLAUDE.md §Canon, docs/subsystems/agents/cognition/README.md,
  # WORKBENCH/melody-hart-program/README.md §2.

  - term_id: graph
    canonical_id: engineering:graph
    canonical_name: "Graph (Execution)"
    pref_label: "graph"
    synonyms: ["execution graph", "graph template", "LangGraph graph"]
    short_definition: "A compiled LangGraph execution template — in-process orchestration with slots filled from the node registry."
    definition: >
      A compiled LangGraph execution template (shape from graph_templates,
      slots filled from the node registry). Graphs orchestrate calls at the
      runtime layer: in-process, single-actor, seconds-to-minutes. The
      cognitive loop IS a graph; loops iterate inside graphs; a run invokes
      a graph. Graphs do not cross actor boundaries — that is the SOP's job.
    scope_note: >
      FAS runtime sense only (FAS canon, locked 2026-07-11). Refers to a
      LangGraph execution graph, not a mathematical graph, knowledge graph,
      or org chart.
    related_terms: [engineering:cognitive-loop, engineering:loop, engineering:engine, operations:layer-rule]
    status: draft
    axis: IS
    term_type: structural_concept

  - term_id: cognitive-loop
    canonical_id: engineering:cognitive-loop
    canonical_name: "Cognitive Loop"
    pref_label: "cognitive loop"
    short_definition: "The graph template an agent executes per message or trigger: Orient → Think → Verify → Execute → Deliver."
    definition: >
      The canonical graph template (engineering:graph) an agent executes for
      each message or trigger: five stages (Orient → Think → Verify →
      Execute → Deliver) with adaptive tiers. Its nodes implement Prepare,
      Reason, Reflect, and tooling steps. Loop iterations (engineering:loop)
      live inside Think/Reflect nodes. Each cognitive-loop execution
      corresponds to one FWE run.
    related_terms: [engineering:graph, engineering:loop, operations:run, operations:layer-rule]
    status: draft
    axis: both
    term_type: structural_concept

  - term_id: loop
    canonical_id: engineering:loop
    canonical_name: "Loop"
    pref_label: "loop"
    synonyms: ["reasoning loop"]
    short_definition: "Model-driven iteration inside a graph — the LLM decides the next step (tool calls, Reflect→Think revision cycles)."
    definition: >
      A model-driven iteration cycle inside a graph execution
      (engineering:graph). The LLM itself decides each next step: Think's
      tool-call cycles, Reflect-to-Think revision passes. Loops are not
      separate graphs — they live within a graph's nodes. "A loop agent"
      is simply a graph whose core is one large model-driven cycle. Loops
      iterate; graphs orchestrate; both are in-process and single-actor.
    related_terms: [engineering:graph, engineering:cognitive-loop, operations:layer-rule]
    status: draft
    axis: HAPPENS
    term_type: structural_concept

  - term_id: engine
    canonical_id: engineering:engine
    canonical_name: "Engine"
    pref_label: "engine"
    short_definition: "A packaged multi-step automation invoked as a unit — typically a deterministic graph with LLM steps."
    definition: >
      A packaged multi-step automation invoked as a named unit
      (engine_invocations). Engines are typically deterministic graphs with
      embedded LLM steps, designed to be called like a function from within
      a cognitive loop. Unlike a general graph, an engine has a stable
      input/output contract and is reusable across agents and tenants.
    related_terms: [engineering:graph, operations:run, operations:layer-rule]
    status: draft
    axis: IS
    term_type: structural_concept

# Non-polysemous engineering terms migrate from legacy glossary.yaml
# (concept_scheme: engineering, ~163 terms) during the sub-scope.
