# Auto-generated by JOS charter sync. DO NOT EDIT.
# Generated at: 2026-05-17T19:37:17.104698+00:00
# Purpose: JOS guidance module (per JOS-SPEC-014). Agents: filter on `applicable_when:` tags against the current work context; honor `do:`/`avoid:` precedence per JOS-D0057.
# Source of truth: jordan-operating-system repo.
# Edits to this file will be overwritten on the next sync.

# JOS Guidance — Federation Configuration
# Per JOS-D0057 (Guidance as First-Class Lens) + JOS-SPEC-014
# (Guidance Enumeration Manifest). Federation owner: JOS.
#
# Architecture mirrors the federated glossary (SPEC-013): core + 10
# domain modules with full-name prefixes (no abbreviations per JOS-S31
# v1.1). Each per-domain file enumerates guidance INSTRUMENTS (their
# canonical IDs, types, and `applicable_when:` tags) — content lives
# in the instrument files themselves; this is the index.

guidance_id: jos-guidance
name: JOS Federated Guidance Index
description: >
  Authoritative federated catalog of JOS guidance instruments
  (principles, concepts, methods, frameworks, playbooks, guides,
  best-practices, suggested-flexibility standards). Agents querying
  "what guidance applies to my work?" consult the per-domain or
  rendered flat-index views, not the source instruments directly.
  Pairs with the federated glossary (SPEC-013) which catalogs terms;
  this catalogs wisdom.

schema_version: "1.0"
schema_file: "_schema.yaml"
tag_vocabulary_file: "_tag-vocabulary.yaml"
federation_owner: jos
owner: tim
default_steward: claude
review_cadence: quarterly

file_strategy: federated

modules:
  - prefix: core
    name: Cross-Cutting Core Guidance
    path: core.yaml
    content_owner: jos
    description: >
      Guidance that applies regardless of domain (e.g., the meta-
      synthesis method, declared-exemplified-guided-governed principle,
      the 1970/2070 meta-test, foundational concepts like fractal
      self-similarity and ambient discoverability).

  - prefix: meta
    name: Meta-Governance Guidance
    path: domains/meta.yaml
    extends: core
    domain: meta
    content_owner: jos

  - prefix: engineering
    name: Engineering Guidance
    path: domains/engineering.yaml
    extends: core
    domain: engineering
    content_owner: jos

  - prefix: documentation
    name: Documentation Guidance
    path: domains/documentation.yaml
    extends: core
    domain: documentation
    content_owner: jos

  - prefix: data
    name: Data Guidance
    path: domains/data.yaml
    extends: core
    domain: data
    content_owner: jos

  - prefix: finance
    name: Finance Guidance
    path: domains/finance.yaml
    extends: core
    domain: finance
    content_owner: jos

  - prefix: strategy
    name: Strategy Guidance
    path: domains/strategy.yaml
    extends: core
    domain: strategy
    content_owner: jos

  - prefix: operations
    name: Operations Guidance
    path: domains/operations.yaml
    extends: core
    domain: operations
    content_owner: jos

  - prefix: people
    name: People Guidance
    path: domains/people.yaml
    extends: core
    domain: people
    content_owner: jos

  - prefix: legal
    name: Legal Guidance
    path: domains/legal.yaml
    extends: core
    domain: legal
    content_owner: jos

  - prefix: business
    name: Business Operations Guidance
    path: domains/business.yaml
    extends: core
    domain: business
    content_owner: jos
