# foundry: kind=service domain=client-intelligence-platform touches=integration
"""CIP connector reference implementations.

Each connector lives under ``cip/integration_mesh/connectors/<connector_name>/``.
The ``fixture`` connector is the canonical reference implementation; future
real connectors (zendesk, hubspot, etc.) follow the same shape.
"""
